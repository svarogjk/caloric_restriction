---
name: volume_sync_2026_07_31
description: Synced 69 dataset files and 8 platform mapping files to production (391 MB total)
metadata:
  type: project
---

## Sync Event: 2026-07-31

**Status:** ✓ Complete

**Delta:**
- `/app/datasets`: 365 → 434 files (+69, ~385 MB)
  - New GEO accessions: GSE108492, GSE14814, GSE166998, GSE175692, GSE186102, GSE199633, GSE200864, and 62 others
  - Each dataset includes: `.parquet`, `.metadata.parquet`, `.gene_mapping.parquet`, `.metrics.json`, `.strategy.json` (5 files per accession)

- `/app/platform_mappings`: 91 → 99 files (+8 synced, 6 pre-existing server-only)
  - Synced: GPL10481, GPL13607, GPL13695, GPL26936, GPL3730, GPL8264, GPL8269, GPL8274 (all `_gene_mapping.parquet`)
  - Server-only (production-generated): GPL1291, GPL13376, GPL17226, GPL2834, GPL6106, GPL8432

**Transfer method:**
1. rsync datasets (385 MB) to `/opt/geo-survival/seed/datasets/`
2. rsync platform_mappings (6 MB) to `/opt/geo-survival/seed/platform_mappings/`
3. `docker compose cp` both into running container (app_datasets + app_platform_mappings volumes)
4. Cleaned up staging directory

**Verification:** Post-sync file counts match or exceed local (no service restart required — live `Path.exists()` checks per request).

**Safety notes:**
- Never touched `app_data` volume or production SQLite database
- All transfers additive-only (no overwrites)
- Server retained 6 production-generated mappings that don't exist locally (expected, never delete)
